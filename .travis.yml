language: node_js
node_js:
  - '10'
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
script:
  - cd web
  - npm install
  - npm run build.prod
install: 
  - npm install -g firebase-tools
after_success:
  - firebase deploy --token $FIREBASE_TOKEN